apiVersion: v1
kind: Service
metadata:
  name: audio-transcriber-api
  namespace: transcription
spec:
  type: ClusterIP
  selector:
    app: audio-transcriber-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
